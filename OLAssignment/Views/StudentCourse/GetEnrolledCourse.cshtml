@model IEnumerable<OLAssignment.Models.StudentCourse>

@{
    ViewBag.Title = "GetEnrolledCourse";
}

<h2>GetEnrolledCourse</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CourseId.CourseId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CourseId.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.CourseId.CourseId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.CourseId.CourseName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Status)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.RowId }) |
        @*@Html.ActionLink("Details", "Details", new { id=item.RowId }) |*@
        @*@Html.ActionLink("Delete", "Delete", new { id = item.RowId })*@
    </td>
</tr>
}

</table>
